#!/bin/sh

set -e

. /usr/share/debconf/confmodule

_CONFFILE="/etc/default/default"

if [ -e "${_CONFFILE}" ]
then
	. ${_CONFFILE} || true

	db_set lxc/auto "${LXC_AUTO}"
fi

db_settitle lxc/title
db_input low lxc/auto "${LXC_AUTO}" || true
db_go

db_stop
